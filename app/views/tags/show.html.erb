<div id="all_tags">
  <%= link_to "All Tags", tags_path %>
</div>

<div id="tag_info">
  <h1>Files tagged "<%= @tag.name %>"</h1>
</div>

<div id="file_list">
  <% @tag_files.each do |file| %>
    <%= link_to "#{file.directory.name}/#{file.name}", file %><br />
  <% end %>
</div>
